#!/usr/bin/env bash

set -euo pipefail

if [ $# -ne 2 ]; then
	echo 'Usage: git-commit-num <commit-id (max 8 digits)> <log-file>' >&2
	exit 1;
fi

COMMIT_ID=$1
LOG_FILE=$2

grep -nr "$COMMIT_ID" "$LOG_FILE"
